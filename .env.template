# --- Planner Backend Configuration ---
# Supabase Database Settings
DB_HOST=your-supabase-project.pooler.supabase.com
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password

# Supabase API Settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Zoho Academia Integration (Manual Session)
# Note: These values expire and usually need to be captured from a browser session
ZOHO_CSRF_TOKEN=your-captured-csrf-token
ZOHO_COOKIE=your-captured-session-cookie

# --- IoT Projects Configuration (Arduino/ESP32) ---
# Create a config.h file in the respective project folder with these values
# #define WIFI_SSID "your_ssid"
# #define WIFI_PASSWORD "your_password"
# #define FIREBASE_HOST "your-project.firebaseio.com"
# #define FIREBASE_AUTH "your-auth-token"
